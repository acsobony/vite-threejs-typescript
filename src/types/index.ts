// Custom type definitions

// Example custom type (not used yet, but here for future reference)
export interface CustomCubeConfig {
  size: number;
  color: number;
  metalness: number;
  roughness: number;
  position: {
    x: number;
    y: number;
    z: number;
  };
}